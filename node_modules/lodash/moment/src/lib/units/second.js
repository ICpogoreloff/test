import { makeGetSet } from 'lodash/moment/src/lib/moment/get-set';
import { addFormatToken } from 'lodash/moment/src/lib/format/format';
import { addUnitAlias } from 'lodash/moment/src/lib/units/aliases';
import { addUnitPriority } from 'lodash/moment/src/lib/units/priorities';
import { addRegexToken, match1to2, match2 } from 'lodash/moment/src/lib/parse/regex';
import { addParseToken } from 'lodash/moment/src/lib/parse/token';
import { SECOND } from 'lodash/moment/src/lib/units/constants';

// FORMATTING

addFormatToken('s', ['ss', 2], 0, 'second');

// ALIASES

addUnitAlias('second', 's');

// PRIORITY

addUnitPriority('second', 15);

// PARSING

addRegexToken('s', match1to2);
addRegexToken('ss', match1to2, match2);
addParseToken(['s', 'ss'], SECOND);

// MOMENTS

export var getSetSecond = makeGetSet('Seconds', false);
